<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.daoha.infrastructure.dao.IOrderDao">

    <resultMap id="dataMap" type="top.daoha.infrastructure.dao.po.PayOrder">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="order_id" property="orderId"/>
        <result column="order_time" property="orderTime"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="pay_url" property="payUrl"/>
        <result column="pay_time" property="payTime"/>
        <result column="market_type" property="marketType"/>
        <result column="market_deduction_amount" property="marketDeductionAmount"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="top.daoha.infrastructure.dao.po.PayOrder">
        insert into `s-pay-mall-ddd-market`.pay_order(
        user_id, product_id, product_name, order_id, order_time,
        total_amount, status, market_type, market_deduction_amount, pay_amount, create_time, update_time)
        values(
        #{userId}, #{productId}, #{productName}, #{orderId}, #{orderTime},
        #{totalAmount}, #{status}, #{marketType}, #{marketDeductionAmount}, #{payAmount}, now(), now())
    </insert>

    <select id="queryUnPayOrder" parameterType="top.daoha.infrastructure.dao.po.PayOrder" resultMap="dataMap">
        select product_id, product_name, order_id, order_time, total_amount, status, pay_url,market_type, market_deduction_amount, pay_amount
        from `s-pay-mall-ddd-market`.pay_order
        where user_id = #{userId} and product_id = #{productId}
        order by id desc
        limit 1
    </select>

    <update id="updateOrderPayInfo" parameterType="top.daoha.infrastructure.dao.po.PayOrder">
        update `s-pay-mall-ddd-market`.pay_order set pay_url = #{payUrl}, status = #{status},market_type=#{marketType},
        market_deduction_amount=#{marketDeductionAmount},pay_amount=#{payAmount},update_time = now()
        where order_id = #{orderId}
    </update>

    <update id="changeOrderPaySuccess" parameterType="top.daoha.infrastructure.dao.po.PayOrder">
        update `s-pay-mall-ddd-market`.pay_order set status = #{status}, pay_time = now(), update_time = now()
        where order_id = #{orderId}
    </update>

    <update id="changeOrderClose" parameterType="java.lang.String">
        update `s-pay-mall-ddd-market`.pay_order set status = 'CLOSE', pay_time = now(), update_time = now()
        where order_id = #{orderId}
    </update>
    <update id="changeOrderMarketSettlement" parameterType="java.util.List">
        update `s-pay-mall-ddd-market`.pay_order set status = 'MARKET',update_time = now()
        where order_id in
        <foreach item="orderId" collection="outTradeNoList" open="(" close=")" separator=",">
            #{orderId}
        </foreach>

    </update>

    <update id="changeOrderDealDone" parameterType="java.lang.String">
        update `s-pay-mall-ddd-market`.pay_order set status = 'DEAL_DONE', update_time = now()
        where order_id = #{orderId}
    </update>

    <select id="queryTimeoutCloseOrderList" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT order_id as orderId FROM `s-pay-mall-ddd-market`.pay_order
        WHERE status = 'PAY_WAIT' AND NOW() >= order_time + INTERVAL 30 MINUTE
        ORDER BY id ASC
        LIMIT 50
    </select>

    <select id="queryNoPayNotifyOrder" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT order_id as orderId FROM `s-pay-mall-ddd-market`.pay_order
        WHERE status = 'PAY_WAIT' AND NOW() >= order_time + INTERVAL 1 MINUTE
        ORDER BY id ASC
        LIMIT 10
    </select>
    <select id="queryOrderByOrderId" parameterType="java.lang.String" resultMap="dataMap">
        select user_id,product_id, product_name, order_id, order_time, total_amount, status, pay_url,market_type, market_deduction_amount, pay_amount
        from `s-pay-mall-ddd-market`.pay_order
        where order_id = #{orderId}
    </select>
    <select id="queryUserOrderList" resultMap="dataMap">
        SELECT
        id, user_id, product_id, product_name, order_id, order_time, total_amount, status,
        pay_url, pay_time, market_type, market_deduction_amount, pay_amount
        /* 其他你需要的字段 */
        FROM `s-pay-mall-ddd-market`.pay_order
        WHERE user_id = #{userId}

        <!-- 动态处理状态集合 -->
        <if test="dbStatusList != null and dbStatusList.size() > 0">
            AND status IN
            <foreach collection="dbStatusList" item="status" open="(" separator="," close=")">
                #{status}
            </foreach>
        </if>

        <!-- 滚动分页逻辑 -->
        <if test="lastId != null">
            AND id &lt; #{lastId}
        </if>

        ORDER BY id DESC
        LIMIT #{pageSize}
    </select>

    <select id="queryUserOrderStatistics" resultType="top.daoha.domain.order.model.valobj.OrderCount">
        SELECT
        status AS status,
        COUNT(*) AS count
        FROM `s-pay-mall-ddd-market`.pay_order
        WHERE user_id = #{userId}
        GROUP BY status
    </select>

    <select id="queryOrderByUserIdAndOrderId" resultMap="dataMap">
        select id, user_id, product_id, product_name, order_id, order_time, total_amount, status, pay_url, pay_time, market_type, market_deduction_amount, pay_amount
        from `s-pay-mall-ddd-market`.pay_order
        where user_id = #{userId} and order_id = #{orderId}
    </select>

    <update id="refundOrder">
        update `s-pay-mall-ddd-market`.pay_order
        set status = 'CLOSE', update_time = now()
        where user_id = #{userId} and order_id = #{orderId}
    </update>
</mapper>
